name: eiho_one
description: "Eiho One - Lamka's everything-in-one local business app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.2.0
  flutter_secure_storage: ^9.2.0
  cached_network_image: ^3.3.0
  google_maps_flutter: ^2.5.0
  google_sign_in: ^6.2.0
  url_launcher: ^6.1.0
  share_plus: ^7.0.0
  razorpay_flutter: ^1.4.5
  geolocator: ^13.0.2
  firebase_core: ^3.12.0
  firebase_messaging: ^15.2.0
  flutter_local_notifications: ^18.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
